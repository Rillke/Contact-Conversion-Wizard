name: build

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@master
    - run: dotnet build source/Contact-Conversion-Wizard.sln --configuration Release
    - run: cd source/bin/Release/ && dir
